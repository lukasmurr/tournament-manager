import { ChangeDetectionStrategy, Component } from '@angular/core';

@Component({
  selector: 'app-config',
  standalone: true,
  template: '<p>Config component to be implemented next.</p>',
  changeDetection: ChangeDetectionStrategy.OnPush,
})
export class ConfigComponent {}
